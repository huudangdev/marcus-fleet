# Marcus Fleet Legacy Memory Shim

This file exists for backward compatibility with older `.agents` skills,
workflows, and docs that still reference `.agents/agents.md`.

Primary project memory now lives at:

- `../agents.md`

Read and update `../agents.md` as the source of truth.
Use this shim only as a compatibility pointer, not as an independent memory log.
