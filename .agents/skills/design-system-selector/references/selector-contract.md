# Design System Selector Contract

Use this reference when `design-system-selector` binds features to domain design systems.

## 1. Domain Mapping
- `company` (default)
- `erp-enterprise`
- `ops-monitoring`
- `executive-insight`
- `binance`

## 2. Zero-Fabrication Rule
Every component and layout must bind strictly to the chosen `DESIGN.md` and `tokens.json`.
