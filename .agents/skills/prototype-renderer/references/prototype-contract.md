# Prototype Renderer Contract

Use this reference when generating interactive prototypes and component galleries.

## 1. Artifacts
- `prototype.html`: Interactive clickable prototype with state toggles.
- `components.html`: Isolated component gallery showcasing all variants and states.

## 2. Token Compliance
All components must consume active tokens from `tokens.json` without fabrication.
