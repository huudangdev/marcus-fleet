---
description: Marcus Fleet Enterprise SDLC Phase 2 V35 (Interactive Prototype & Component Gallery Renderer)
---

# 🚀 INTERACTIVE PROTOTYPE & COMPONENT RENDERER (`/design.prototype`)

> **CORE ARCHITECTURE MANDATE:**  
> Render interactive HTML `prototype.html` and `components.html` with route switchers and 8-state simulator controls for 1-3 primary flows.

// turbo-all

---

## ⬛ SYSTEMIC MEMORY & STATE INHERITANCE

<state_propagation_boundary>
**DOGMATIC STATE PROPAGATION:** Requires approved `board.html` and `state-coverage.md`.
</state_propagation_boundary>

---

## 🔲 DAG TOPOLOGY: CHRONOLOGICAL EXECUTION NODES

### 🔵 NODE 0: PROTOTYPE & COMPONENT HTML RENDER
* *🔗 Input Vector:* `board.html`, `state-coverage.md`
* *🧠 Injected Tensors:* `prototype-renderer`, `benny-frontend-engineer`
* *📦 Emitted Artifacts:* `prototype.html`, `components.html`

**[Execution Protocol]:**
1. Render interactive prototype shell (`prototype.html`) with screen switcher and state toggles.
2. Render component gallery (`components.html`) showing variants and states.

---

### 🟢 NODE 1: TOKEN & COMPONENT COMPLIANCE AUDIT
* *🔗 Input Vector:* `prototype.html`, `components.html`
* *📦 Emitted Artifacts:* Audit logs

**[Execution Protocol]:**
1. Run `python3 .agents/scripts/validate_token_compliance.py --feature <feature-path>`.
2. Run `python3 .agents/scripts/validate_component_compliance.py --feature <feature-path>`.
3. Present prototype link to operator for testing. Instruct to run `/design.review`.
